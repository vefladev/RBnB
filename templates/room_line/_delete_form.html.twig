<form method="post" action="{{ path('app_room_line_delete', {'id': room_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room_line.id) }}">
    <button class="btn">Delete</button>
</form>
